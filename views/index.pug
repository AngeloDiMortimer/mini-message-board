extends layout

block content
  h1= title
  p Welcome to my #{title}

  each message in messages
    h3 #{message.user} 
    p #{message.text} 
    p #{message.added}
  
  a(href="/new") Send a message
